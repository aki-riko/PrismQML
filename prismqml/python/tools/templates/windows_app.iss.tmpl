; SPDX-License-Identifier: MIT
; Generated template is part of PrismQML, licensed under MIT.
; 生成模板是 PrismQML 的一部分，采用 MIT 许可证授权。
; Generated by prismqml-installer. DO NOT EDIT.
; Source manifest: $MANIFEST_NAME

#define PrismAppName "$APP_NAME"
#define PrismAppVersion "$APP_VERSION"
#define PrismAppPublisher "$APP_PUBLISHER"
#define PrismAppExe "$APP_EXECUTABLE"
#define PrismAppUserModelID "$APP_USER_MODEL_ID"
#define PrismDistDir "$DIST_DIR"
#define PrismChineseMessagesFile "$CHINESE_MESSAGES_FILE"
$APP_URL_DEFINE
$SETUP_ICON_DEFINE

[Setup]
; A stable AppId makes every version upgrade the same installation. 固定 AppId 保证原位升级。
AppId={{$APP_ID}
AppName={#PrismAppName}
AppVersion={#PrismAppVersion}
AppPublisher={#PrismAppPublisher}
$APP_URL_LINES
DefaultDirName=$DEFAULT_DIR
DefaultGroupName={#PrismAppName}
UsePreviousAppDir=yes
DisableProgramGroupPage=yes
OutputDir=$INSTALLER_OUTPUT_DIR
OutputBaseFilename=$OUTPUT_NAME
$SETUP_ICON_LINE
UninstallDisplayIcon={app}\{#PrismAppExe}
Compression=lzma2
SolidCompression=yes
WizardStyle=modern
ShowLanguageDialog=auto
LanguageDetectionMethod=uilanguage
ArchitecturesAllowed=x64compatible
ArchitecturesInstallIn64BitMode=x64compatible
PrivilegesRequired=$PRIVILEGES
; Silent next-launch contract: close old files, never restart the app. 静默覆盖且不自动重启应用。
CloseApplications=yes
RestartApplications=no

[Languages]
Name: "chinesesimplified"; MessagesFile: "{#PrismChineseMessagesFile}"
Name: "english"; MessagesFile: "compiler:Default.isl"

[Tasks]
Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked

[Files]
Source: "{#PrismDistDir}\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs

[Icons]
Name: "{group}\{#PrismAppName}"; Filename: "{app}\{#PrismAppExe}"; IconFilename: "{app}\{#PrismAppExe}"; AppUserModelID: "{#PrismAppUserModelID}"
Name: "{group}\{cm:UninstallProgram,{#PrismAppName}}"; Filename: "{uninstallexe}"
Name: "{autodesktop}\{#PrismAppName}"; Filename: "{app}\{#PrismAppExe}"; IconFilename: "{app}\{#PrismAppExe}"; AppUserModelID: "{#PrismAppUserModelID}"; Tasks: desktopicon

[Run]
; Interactive installs may offer launch; silent updates stay stopped until next user launch.
; 交互安装可选择启动；静默更新保持退出，等待用户下次启动。
Filename: "{app}\{#PrismAppExe}"; Description: "{cm:LaunchProgram,{#PrismAppName}}"; Flags: nowait postinstall skipifsilent

$EXTENSION_INCLUDE
